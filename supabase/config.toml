project_id = "brpqtaaknxdqkjvzfvlo"

[functions.ingest-signal]
verify_jwt = false

# Pipeline functions: triggered server-side by the nightly pg_cron jobs (and the
# owner-gated Run scan button). They use the service role internally; RLS + the
# RLS-lockdown migration protect the data, so they don't require a user JWT.
[functions.signal-collect]
verify_jwt = false

[functions.signal-process]
verify_jwt = false

# Roadmap drafting: admin-triggered from the board; reasons over live rows via
# the AI Gateway and persists with the service role.
[functions.opportunity-roadmap]
verify_jwt = false

# W6 dogfood loop: admin-triggered from the board; runs a feature candidate back
# through the v1 Simulator (simulate-idea + 3 personas) and persists with the
# service role. Same posture as opportunity-roadmap (no user JWT required).
[functions.signal-candidate-deepen]
verify_jwt = false
